<%= render 'books/book', user: @user, book: @book %>
              <div class="col-8 offset-md-1">
                <h2>Books</h2>
                <table class="table table-hover table-inverse">
                  <thead>
                    <tr>
                      <th></th>
                      <th>title</th>
                      <th>Opinion</th>
                      <th colspan="3"></th>
                    </tr>
                  </thead>
                  <tbody>
                    <% @books.each do |book| %>
                      <tr>
                        <td><%#= attachment_image_tag book.user.id, :profile_image, fallback: "no_image1.jpg"%></td>
                        <td><%= book.title %></td>
                        <td><%= book.body %></td>
                      </tr>
                    <% end %>
                  </tbody>
                </table>

            </div>
        </div>
      </div>
    </main>

  </body>
  </html>